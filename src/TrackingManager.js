'use strict';

/**
 * This exposes the native TrackingManager module as a JS module.
 *
 * See README.md for the API documentation.
 */
import { NativeModules } from 'react-native';

module.exports = NativeModules.TrackingManager;
